#!/usr/bin/env node

const result = require('../lib/cli')(process.argv.slice(2))

if (result instanceof Promise) {
  result.then(result => console.log(result), error => console.error(error))
} else if (result !== undefined) {
  console.log(result)
}
